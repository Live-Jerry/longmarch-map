# 修改记录 V005

> 记录时间: 2026-07-16
> 基于 V004 版本的修改

---

## 1. 星火拾遗 — 去掉登录要求

### 背景
用户提交星火内容（民间史料、照片、录音等）时，原先要求用户必须登录。根据产品决策，改为无需登录即可提交。

### 改动文件

| 文件 | 修改 |
|------|------|
| `api/spark_api.py` | `submit_spark()` 去掉 `AuthManager.current_user()` 的 None 检查；`create_full_spark()` 去掉 `require_login()`，改为可选获取用户 |
| `static/js/spark.js` | 提交 `/full` 时，有 token 才发 Authorization 头，无 token 时省略 |
| `interfaces/spark_interface.py` | 更新接口文档，标注"公开（无需登录）"，补充 `/full` 端点文档 |

### 关键逻辑
- 登录用户提交：`user_id` 存用户 ID
- 匿名用户提交：`user_id` 存 `NULL`
- 提交者信息（姓名/电话）通过表单字段 `submitter_name` / `submitter_phone` 收集，存入 `content` JSON
- 无论是否登录，所有提交均进入 `pending` 状态，需管理员审核
- 数据库 `spark` 表的 `user_id` 列已是 nullable，无需迁移

---

## 2. 认证系统 — 弹窗集成注册/忘记密码

### 背景
原来登录按钮跳转独立 `/login` 页面，注册有独立 `/register` 页面。改为在主页右侧控制栏点击登录按钮时，打开弹窗（modal），弹窗内集成三个标签页：登录、注册、忘记密码。

### 改动文件

| 文件 | 修改 |
|------|------|
| `api/auth_api.py` | 新增 `POST /api/v1/auth/reset-password` 端点 |
| `services/auth_manager.py` | 新增 `reset_password()` 方法 |
| `models/user.py` | 新增 `update_password()` 类方法 |
| `static/js/auth.js` | 新增弹窗控制函数、三个表单处理器、`resetPassword()` API 函数；改造 `updateAuthUI()` |
| `templates/index.html` | 新增认证弹窗 HTML（登录/注册/忘记密码三个表单页面，底部链接导航切换） |
| `static/css/style.css` | 新增 `.auth-form-panel` 样式 |

### 新 API

```
POST /api/v1/auth/reset-password
请求: { "username": "...", "new_password": "..." }
响应: { "code": 0, "message": "密码重置成功，请使用新密码登录" }
```

### 用户流程
1. 点击右侧「 登录」→ 打开认证弹窗
2. 弹窗三个标签页：登录 / 注册 / 忘记密码
3. 注册成功自动登录并关闭弹窗
4. 忘记密码重置成功后自动切回登录页
5. 登录后按钮变为「 退出」，单击退出

### 兼容性
- 独立 `/login` 和 `/register` 页面仍然保留，可直接访问
- 独立页面与弹窗共用 `auth.js` 中的 `login()` / `register()` / `resetPassword()` 函数

### 设计变更
- 去掉顶部三个标签页按钮（登录/注册/忘记密码），改为底部文字链接切换
- 用户流程：登录页底部「立即注册」「忘记密码？」→ 注册页底部「立即登录」→ 重置页底部「← 返回登录」
- 更符合常见弹窗设计惯例

---

## 3. 用户手册 — 新增

### 改动文件
| 文件 | 内容 |
|------|------|
| `004 项目文档/长征文化用户手册.md` | 完整用户手册（产品理念、功能介绍、操作说明） |
| `templates/help.html` | 用户手册的 HTML 渲染模板 |
| `app.py` | 新增 `/manual` 页面路由 |
| `templates/index.html` | 新增帮助按钮 |

### 修复记录（V005.1）

| 问题 | 原因 | 修复 |
|------|------|------|
| 帮助页不能滚动 | `style.css` 的 `html,body {overflow:hidden;height:100%}` 锁定视口 | `html {overflow:visible !important}` + `<script>` JS 强制覆盖 |
| 帮助页无退出按钮 | 返回按钮在文档末尾，被 cut off | 添加 `position:fixed;bottom:0` 底部悬浮栏，含「← 返回地图」按钮 |
| 浏览器缓存旧 JS | `spark.js` 未加版本号 | 所有 JS 加 `?v=20260716` 缓存破坏参数 |
| 浏览器缓存帮助页 | 页面无 Cache-Control 头 | `/manual` 路由返回 `Cache-Control: no-store` + `Pragma: no-cache` |
| `app.py` 重复代码 | `_register_blueprints()` 定义两次 | 删除重复定义，合并代码 |
| 登录按钮打不开 | `initAuth()` 无 token 时直接 `return`，导致 `updateAuthUI()` 从未执行，按钮没有绑定 onclick | 移除 early return，末尾无条件调用 `updateAuthUI()` |
| 修改节点模式不能提交 | `#spark-fields-new` 中 `required` 字段被 CSS 隐藏后，浏览器校验仍然静默阻止提交 | `switchSparkTab()` 中切换时动态添加/移除 `required` 属性 |
| 管理后台节点新增/编辑/查看失效 | 编辑链接指向不存在的路由；JS 动态生成弹窗代码脆弱 | 创建 `_node_dialog.html` + `_node_form_fields.html` 共享模板，admin/nodes.html 通过 `{% include %}` 复用同一份代码。增/改/查同一表单，node_id 编辑时只读、查看时全字段 disabled。注意：Jinja2 注释中不能用 `{% raw %}{% include %}{% endraw %}` 字样避免无限递归 |
