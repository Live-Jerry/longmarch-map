# 2026-07-30

## EC变更跟踪表填写规则确认

- 只能写 F(影响模块)、G(原因分析)、H(变更方案)、I(状态) 四列
- 不碰用户写的列 (1-5、9-10中的状态文字以外部分的确认列？不对，I=9=状态是我该写的)

## CHANGELOG 修复

修复了之前的覆盖错误，恢复 V2.1.0 内容，正确插入 V2.1.1

## 版本管理文档 V3.1 修订

完成了术语统一（服器Dev/服务器prd）、Tag格式统一（CZprd+版本号+last-good / CZdev+版本号+protect）、工作流统一。详见文档。

## EC第20条 - 实施完成

问题：重复点击开始按钮产生多个红点
根因：startAutowalk() 不清理旧标记和动画
方案：A. 开头清理旧标记/动画 B. 末尾禁用开始按钮
状态：已修复，待用户确认测试
改动文件：static/js/controls.js (两处)

## 路线点显示问题 - 调查（未解决）

用户报：军队路线显示有问题，"节点之间的漫游坐标点的名字，都不见了"

调查过程：
1. 错误地查后端数据库和API → 用户批评（显示问题不该查后端）
2. 查前端代码：loadRoutes() 只画polyline，不创建路线点标记
3. V2.1.0 (5f99b9d) 改动：删除了节点bindPopup、删除了起点终点circleMarker
4. 发现了 `.route-point-label` CSS类（V2.1.0添加）但没有JS代码创建此类元素
5. 服器Dev上验证：DOM中无路线点标注元素，节点旗帜标记正常
6. 结论：中间路线点（云石山、马道口等）的地图标注在代码层面从未实现过

问题未解决，用户仍在等待回答。陷入僵局。

## 生产升级流程状态

EC#20 实施：已完成代码、本地测试、服器Dev测试、git push、CHANGELOG
升级流程卡在第5.1步：等待用户确认"执行备份生产"

当前状态：停留在 V2.1.1 版本发布流程的第5.1步，用户未确认。
