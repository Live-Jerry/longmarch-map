# 2026-08-05

## 标题栏新增"知常轩"文字（V004）

- 需求：在 logo 标题图片右侧加文字"知常轩"
- 修改：
  - `templates/index.html` — `.title-bar-left` 内 img 后新增 `<span class="brand-name">知常轩</span>`；样式表版本号 v=20260725_6 → v=20260805_1
  - `static/css/style.css`（实际生效文件）— `.title-bar-left` 布局由 column 改为 row + align-items:center；新增 `.brand-name` 样式（楷体、22px、浅灰、左侧竖线分隔、字距4px）；768px 媒体查询内缩小为 14px
  - 误改的 `templates/style.css`（旧版未使用文件）已还原，未保留改动
- 测试：本地 venv 启动 Flask 验证通过 — HTML/CSS 均含 brand-name，Edge headless 截图确认"知常轩"显示在 logo 右侧、垂直居中、带分隔线
- 已提交并部署：commit 3d125b4（feat: 标题栏 logo 右侧新增「知常轩」标识文字）push 到 origin/develop（连同之前 3 个未推送提交一并推送）；服务器 /opt/longmarch-dev git pull 成功，systemctl restart longmarch-dev，服务 active
- 部署验证：dev.zhichangxuan.com 返回 200，HTML 含 brand-name，CSS 含 2 条 brand-name 规则，Edge headless 截图确认显示正常
- 生产部署（2026-08-06）：生产服务器 /opt/longmarch-map 不能直接 pull（本地 develop 有独有 ICP 提交 74a6276 且未推送，工作区有未提交的 longmarch.db 生产数据和 deploy.sh 修改，origin/develop 引用是旧的）→ 改用精确补丁方案：python 脚本锚点校验后只改 index.html（加 brand-name span + css 版本号 v=20260806_1）和 style.css（brand-name 规则 + 媒体查询），备份在 /tmp/index.html.bak_20260806、/tmp/style.css.bak_20260806；不动数据库和 deploy.sh
- 生产验证：zhichangxuan.com 200，HTML 含知常轩，CSS 含 2 条规则，截图确认显示正常且 ICP 页脚未受影响；longmarch-map 服务 active
