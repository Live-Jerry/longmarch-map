# 2026-08-06 — 生产 git 正规化同步

## 事件起因
- 用户测试通过后要求"按测试环境部署生产"。此前 8-05/8-06 加知常轩时在服务器上直接改文件（补丁方式），未走 git，生产仓库遗留未提交改动，导致无法直接 pull。用户批评生产与测试不一致、破坏规则。

## 核查结果（服务器实测）
- 生产 /opt/longmarch-map 与 dev /opt/longmarch-dev 的 001 项目源码：static 无差异；templates 仅 index.html 差异（css 版本号 v=20260805_1 vs v=20260806_1 + span 空行位置）；数据库结构、行数基本一致（message 6 vs 4、user 4 vs 3 为运行数据差异）。
- 生产"未提交改动"来源：1) longmarch.db 是 git 跟踪的活数据（永远显示 modified）；2) index.html/style.css 是补丁期直接改的；3) deploy.sh 是文件损坏（UTF-8 被按 GBK 写坏成 1 行乱码）。
- 生产分支分叉：生产独有 74a6276（ICP，patch 版 hash），dev 有 3be541b（ICP）+ 后续提交。

## 修复动作（已全部完成）
1. 备份生产 db 到 /tmp/longmarch.db.prd，deploy.sh 备份 /tmp/deploy.sh.prd
2. git checkout 丢弃补丁残留，恢复损坏的 deploy.sh
3. git merge origin/develop --no-edit，仅 CHANGELOG.md 冲突（用 dev 版解决），commit 9144406
4. 恢复生产 db（生产运行数据不丢），重启 longmarch-map，服务 active，页面 200
5. 验证 index.html/style.css 与 dev 逐字节一致，页面含知常轩 + 陕ICP备案号
6. 补齐合规 tag：CZprd+v2.1.2+last-good（升级前生产备份）、CZdev+v2.1.3+rc（dev 测试通过版），已 push 远程（本地 push 成功；服务器 push 报 token 失效，已知问题待解）

## 教训
升级前读版本管理文档。
