# 2026-08-13

## 知常轩资源中心：隐藏 ABC 分级 + 栏目更名（已上线 zcxdev）

### 背景
用户在 zcxdev 资源中心截图发现模块卡片右上角有"多余文字"（竖排蓝色标签，如"A · 必读经典三级导读"）。排查确认：
- 那是资源分级徽章（.resource-grade），模板把内部管控字段 category(A/B/C) 和栏目名拼在一起渲染
- ABC 分级出自《知常轩建站资源盘点表_百度云_V1.0.md》（2026-08-07 盘点）：A=公有领域可直接加工 / B=可作底本需核实版权 / C=现代出版物仅书目导航——纯内部版权筛选依据
- 用户明确指出：资源是否侵权/可用是内部管控，不该告诉用户
- "必读经典三级导读"是设计文档 V2.3 定的栏目名（三级导读=启蒙/进阶/深度分层），但作为栏目名太长太拗口

### 修改内容（用户确认后实施）
1. 设计文档《知常轩_网站建站方案_V2.3.docx》局部修订 V2.3.6：
   - 栏目名「必读经典三级导读（启蒙级/进阶级/深度级）」→「必读经典（启蒙级/进阶级/深度级）」
   - 资源中心页面结构去掉"模块内 ABC 分级"浏览
   - 8.2 步骤3 明确：ABC 分级为内部管控字段（版权筛选依据），仅后台审核/排序使用，不对用户展示
2. app.py COLUMNS['guiji']：必读经典三级导读 → 必读经典
3. templates/resources.html：徽章只显示栏目名（无栏目名不显示徽章），去掉 category 渲染
4. static/css/style.css：.resource-grade 从 28px 圆形改自适应胶囊（padding 3px 10px, border-radius 999px, max-width 60%, ellipsis），默认背景 var(--cui-qing)
5. 数据库（本地+服务器）：resource_link 3 条 + article 2 条 column_name 同步更新

### 测试与部署
- 本地 .venv 启动 app.py（端口 8020），/resources 200，徽章渲染正确（必读经典/古籍精读课/道家/书法入门教程/四大流派精读），无 ABC 字母；/resources/upload 栏目下拉已同步；/article/11 显示"必读经典 · 适龄：7-10"
- 服务器 /opt/zhichangxuan-dev 非 git 部署，直接 scp 3 个文件 + venv python 更新数据库 + systemctl restart zhichangxuan-dev
- 线上 https://zcxdev.zhichangxuan.com/resources 验证 200，徽章只显示栏目名
- 本地 git 提交 62fa72f + 36bc970 已 push develop，CHANGELOG 已记录

### 教训
- 服务器 zcxdev 是直接文件部署（非 git pull），部署时对比基线后 scp 覆盖，注意别覆盖服务器独有文件（templates 下有 admin/article/modules 子目录本地没有）
- PowerShell 引号转义对 ssh 内联命令不友好，复杂命令写 .sh 脚本 scp 上去执行

## 追加（15:57）：角标全部取消
用户看截图后认为胶囊角标仍难看（“四大流派精读”标签溢出卡片边界），要求全部取消。
- resources.html 两处 resource-grade 徽章删除，style.css 删 .resource-grade/.grade-a 样式
- 栏目信息仍存数据库 column_name，仅不再展示（用户已定调：栏目/分级都是内部组织手段，页面只展示资源本身）
- 本地测试 200 无 resource-grade；已部署 zcxdev（scp + restart）；git 提交 1711780 + d174cc6 push develop

## 追加（16:01）：资源中心“已下载却无内容”修复——资源直传服务器
问题根源：resource_link 表里 6 类资源（论语/荀子/增广贤文/二十四史/古诗/碑帖）share_url 为空——原设计是网盘分享链接导航（方案A），链接一直未生成，file_size 的“已下载”只是状态说明。
处理：
- 服务器磁盘 40G 总量/已用 5.4G/可用 32G，空间允许
- 本地上传 3.1GB/461 文件至服务器 static/res/（lunyu 136M / xunzi 772M / zengguang 5M / ershisi 2G / shige 100M / chuxue 88M）
- 新增 /resources/browse/<path> 路由：安全扫描 static/res/ 子目录列文件（防 .. 穿越，404 拦截），模板 resource_files.html 复用 file-list 样式
- 单文件资源（增广贤文 PDF）直接指向 /static/res/...
- 更新本地+服务器数据库 share_url/file_size；部署 scp app.py + 模板 + 重启
- 验证：资源中心 30 处“前往资源”、论语浏览 200、二十四史 200、增广贤文 PDF 200、mp3 audio/mpeg 200、史记 PDF 80MB 200、穿越拦截 404
- git 提交 91377e2 + e6ec6e8 push develop；CHANGELOG 已记录
教训：PowerShell 表格输出有延迟导致目录统计顺序混乱，用 Python os.walk 统计更可靠。

## 追加（18:55）：目录整理第一步——新建 100通用管理
用户决定：新建 D:\长征文化\100通用管理 目录，目前只移动版本管理一个文件（项目版本通用管理规范.docx，V4.1），其余不动（000 脚本策划 不改名，007 项目工具 确认两项目共用保持不动）。
- git mv 004 项目文档/项目版本通用管理规范.docx → 100通用管理/；commit 2120120 push develop
- MEMORY.md 两处引用同步更新（L61 工作区结构、L173 版本管理规范路径）
- 混杂点清单.md 同步记录
后续候选整理（未执行）：000 脚本策划 里 9 个长征设计文档/坐标数据 → 004 项目文档；SemVer/git库清单 等通用文档 → 100通用管理
问题根源：resource_link 表里 6 类资源（论语/荀子/增广贤文/二十四史/古诗/碑帖）share_url 为空——原设计是网盘分享链接导航（方案A），链接一直未生成，file_size 的“已下载”只是状态说明。
处理：
- 服务器磁盘 40G 总量/已用 5.4G/可用 32G，空间允许
- 本地上传 3.1GB/461 文件至服务器 static/res/（lunyu 136M / xunzi 772M / zengguang 5M / ershisi 2G / shige 100M / chuxue 88M）
- 新增 /resources/browse/<path> 路由：安全扫描 static/res/ 子目录列文件（防 .. 穿越，404 拦截），模板 resource_files.html 复用 file-list 样式
- 单文件资源（增广贤文 PDF）直接指向 /static/res/...
- 更新本地+服务器数据库 share_url/file_size；部署 scp app.py + 模板 + 重启
- 验证：资源中心 30 处“前往资源”、论语浏览 200、二十四史 200、增广贤文 PDF 200、mp3 audio/mpeg 200、史记 PDF 80MB 200、穿越拦截 404
- git 提交 91377e2 + e6ec6e8 push develop；CHANGELOG 已记录
教训：PowerShell 表格输出有延迟导致目录统计顺序混乱，用 Python os.walk 统计更可靠。
