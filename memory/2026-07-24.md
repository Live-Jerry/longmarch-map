# 2026-07-24 — 漫游功能修复 + 路线点精确映射

## 修了三个 bug

1. **重头图标** — ⏮ 改成 ⟳（重头.svg）
2. **重头功能不生效** — `restartAutowalk` 清空状态后正确重启漫游。但用户要求保留速度选择弹窗，不能跳过。已恢复：line 78 和 line 620 都调用 `showSpeedSelector()`
3. **漫游途中停靠非节点路线点** — 根源：模糊匹配导致同一 node_id 匹配多个路线点（如 扎西+威信 都到 4.4，安顺+安顺场+石棉 都到 7.1）

## "不要模糊匹配"——重要教训

用户严厉指出三番混淆 安顺（贵州城市）和 安顺场（四川石棉/7.1）。
教训：路线点名 → 节点编号的映射必须精确手动指定，绝不能用 LIKE / 子串搜索。

已创建 `import_exact.py`（本地）→ `import_exact_server.py`（部署用），映射表完全硬编码：
- ARMY1_NODE_MAP: 26 个条目
- ARMY2_NODE_MAP: 9 个条目
- ARMY4_NODE_MAP: 8 个条目
- ARMY25_NODE_MAP: 6 个条目
- 总计 49 个节点标记，每个 army+node_id 组合去重一次

## "服务器"歧义

用户批评我说话不区分 dev/prod。以后：
- **dev** → `/opt/longmarch-dev/` → `dev.zhichangxuan.com`
- **prod** → `/opt/longmarch-map/` → `zhichangxuan.com`
不用"服务器"这个模糊词。

## 当前状态

✅ 路线点精确重新导入（49个节点，无重复）
✅ 重头图标改 ⟳
✅ 速度选择已恢复（首次点漫游 + 重头都弹）
✅ 安顺和安顺场正确区分
✅ 扎西和威信正确去重（只停扎西）
待确认：用户测试 dev.zhichangxuan.com 是否满意
# 2026-07-24 — 漫游功能修复 + 路线点精确映射

## 修了三个 bug

1. **重头图标** — ⏮ 改成 ⟳（重头.svg）
2. **重头功能不生效** — `restartAutowalk` 清空状态后正确重启漫游。但用户要求保留速度选择弹窗，不能跳过。已恢复：line 78 和 line 620 都调用 `showSpeedSelector()`
3. **漫游途中停靠非节点路线点** — 根源：模糊匹配导致同一 node_id 匹配多个路线点（如 扎西+威信 都到 4.4，安顺+安顺场+石棉 都到 7.1）

## "不要模糊匹配"——重要教训

用户严厉指出三番混淆 安顺（贵州城市）和 安顺场（四川石棉/7.1）。
教训：路线点名 → 节点编号的映射必须精确手动指定，绝不能用 LIKE / 子串搜索。

已创建 `import_exact.py`（本地）→ `import_exact_server.py`（部署用），映射表完全硬编码：
- ARMY1_NODE_MAP: 26 个条目
- ARMY2_NODE_MAP: 9 个条目
- ARMY4_NODE_MAP: 8 个条目
- ARMY25_NODE_MAP: 6 个条目
- 总计 49 个节点标记，每个 army+node_id 组合去重一次

## "服务器"歧义

用户批评我说话不区分 dev/prod。以后：
- **dev** → `/opt/longmarch-dev/` → `dev.zhichangxuan.com`
- **prod** → `/opt/longmarch-map/` → `zhichangxuan.com`
不用"服务器"这个模糊词。

## 当前状态

✅ 路线点精确重新导入（49个节点，无重复）
✅ 重头图标改 ⟳
✅ 速度选择已恢复（首次点漫游 + 重头都弹）
✅ 安顺和安顺场正确区分
✅ 扎西和威信正确去重（只停扎西）
待确认：用户测试 dev.zhichangxuan.com 是否满意

---

## Session 后半段（19:34 前）

对 dev.zhichangxuan.com 做了浏览器测试：
1. 点"漫游" → 速度选择弹出 ✓
2. 选速度点"开始" → 从长汀出发 ✓
3. 点"跳过"跳过于都 ✓
4. 点"重头" ⟳ → 速度选择弹出 ✓
5. 需要再选速度+点开始才能回到起点

无新 bug 发现。用户未返回最终确认结果即触发 compaction flush。
