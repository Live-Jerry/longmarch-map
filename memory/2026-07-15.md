# 2026-07-15 日志

## V1.1.0 功能开发记载

### 完成事项

1. **节点图片显示**（V003#9.1.2）
   - 新增 `@app.route("/node-image/<node_id>")` 路由
   - 从 002 项目资源 中按节点编号匹配图片
   - 无图片时返回军旗占位 SVG
   - 图片放在 `panel-image` 区域（固定在面板顶部，不随内容滚动）
   - 占位 SVG 在 001 项目源码/static/images/flag-placeholder.svg

2. **数据清洗：active_sections 标志系统**（V003#3.1+3.3#步骤3）
   - 每个节点新增 `active_sections` JSON 字段，记录哪些内容段有实际内容
   - 清洗 node_data.json：去掉"字段名：无"前缀，空字段设为 null
   - 数据库 node 表新增 `active_sections` TEXT 列
   - 重新导入 29 个节点数据

3. **前端按 active_sections 过滤显示**
   - 5 步主流程（time→battle→meeting→event→significance）只显示 active 的步骤
   - 扩展信息（site→poem→story→people→spark）只显示 active 的字段
   - 步骤指示器只计数 active 步骤
   - 翻页在 active 步骤之间切换

4. **项目版本管理初始化**
   - VERSION: 1.0.0
   - CHANGELOG.md
   - ROADMAP.md（V1.1→V3.0 规划）
   - .gitignore
   - Git 仓库 init + first commit

### 修改的文件

- 001 项目源码/app.py — 新增 CREATE TABLE active_sections 列 + node-image 路由
- 001 项目源码/static/js/nodes.js — 重写至 380 行：active_sections 过滤 + 图片分离 + 步骤跳转
- 001 项目源码/static/css/style.css — 替换节点图片区域样式为 panel-image 固定布局
- 001 项目源码/templates/index.html — 新增 panel-image div
- 001 项目源码/data/node_data.json — 清洗数据 + active_sections
- 001 项目源码/static/images/flag-placeholder.svg — 占位军旗

### 已测试

- [x] Python 语法验证
- [x] API 返回 active_sections 字段
- [x] 有图片节点正确显示
- [x] 无图片节点显示军旗占位
- [x] 无内容字段不显示（如黎平无 poem_article）
# 2026-07-15 日志

## V1.1.0 功能开发记载

### 完成事项

1. **节点图片显示**（V003#9.1.2）
   - 新增 `@app.route("/node-image/<node_id>")` 路由
   - 从 002 项目资源 中按节点编号匹配图片
   - 无图片时返回军旗占位 SVG
   - 图片放在 `panel-image` 区域（固定在面板顶部，不随内容滚动）
   - 占位 SVG 在 001 项目源码/static/images/flag-placeholder.svg

2. **数据清洗：active_sections 标志系统**（V003#3.1+3.3#步骤3）
   - 每个节点新增 `active_sections` JSON 字段，记录哪些内容段有实际内容
   - 清洗 node_data.json：去掉"字段名：无"前缀，空字段设为 null
   - 数据库 node 表新增 `active_sections` TEXT 列
   - 重新导入 29 个节点数据

3. **前端按 active_sections 过滤显示**
   - 5 步主流程（time→battle→meeting→event→significance）只显示 active 的步骤
   - 扩展信息（site→poem→story→people→spark）只显示 active 的字段
   - 步骤指示器只计数 active 步骤
   - 翻页在 active 步骤之间切换

4. **项目版本管理初始化**
   - VERSION: 1.0.0
   - CHANGELOG.md
   - ROADMAP.md（V1.1→V3.0 规划）
   - .gitignore
   - Git 仓库 init + first commit

### 修改的文件

- 001 项目源码/app.py — 新增 CREATE TABLE active_sections 列 + node-image 路由
- 001 项目源码/static/js/nodes.js — 重写至 380 行：active_sections 过滤 + 图片分离 + 步骤跳转
- 001 项目源码/static/css/style.css — 替换节点图片区域样式为 panel-image 固定布局
- 001 项目源码/templates/index.html — 新增 panel-image div
- 001 项目源码/data/node_data.json — 清洗数据 + active_sections
- 001 项目源码/static/images/flag-placeholder.svg — 占位军旗

### 已测试

- [x] Python 语法验证
- [x] API 返回 active_sections 字段
- [x] 有图片节点正确显示
- [x] 无图片节点显示军旗占位
- [x] 无内容字段不显示（如黎平无 poem_article）

---

## 照片填充（V1.1.0 第二阶段）

### 完成事项

1. **29 个节点全部填充历史照片，共 97 张**
   - 来源：百度百科 CDN (`bkimg.cdn.bcebos.com/pic/`) + 百度图片搜索 API (`image.baidu.com/search/acjson`)
   - 文件系统：`002 项目资源/{节点名称}/` 目录
   - 数据库：`media` 表 97 条记录，`type=image, status=approved`
   - 前端 `node-image/<node_id>` 路由直接从 002 目录读取

### 每节点照片数

| 数量 | 节点 |
|------|------|
| 6张 | 通道会议 |
| 5张 | 长汀、黎平、懋功会师 |
| 4张 | 于都、瓮安、遵义 |
| 3张 | 瑞金、湘江、扎西、苟坝、四渡赤水、金沙江、大渡河、夹金山、两河口、芦花、松潘草地、过草地、巴西、俄界、腊子口、哈达铺、榜罗镇、六盘山、吴起镇、会宁、将台堡 |
| 1张 | 泸定桥 |

### 总览

- **002 项目资源** 中相邻文件夹名括号一致，显示为统一格式
- 之前只 4 个节点有照片，现在全部填满
- 部分节点的文件名后缀差异（如 `changting_1.jpg` vs `福建长汀_1.jpg`），不影响功能

### Git

- `c3a98ca` — feat(media): fill all 29 nodes with photos + insert into database

### 清理

- 临时 Python 脚本已删除（test_baike.py, dl_changting.py, dl_baike.py, dl_batch.py, dl_batch2.py, dl_batch3.py, dl_missing.py, dl_tongdao.py, test_dl.py, test_dl2.py, test_api.py, test_app.py, verify_dl.py, check_links.py, node_image_inventory.txt, blanks_report.txt, doc_design_extract.txt）
- `Doxyfile` 中已添加 EXCLUDE 排除 Agent 工作区文件（AGENTS.md, SOUL.md 等）

### 未完成

- 节点名称文件夹内文件名尚未统一标准化
- 尚未将 Doxygen 中的 Agent 文件排除结果截图给用户确认
# 2026-07-15 日志

## V1.1.0 功能开发记载

### 完成事项

1. **节点图片显示**（V003#9.1.2）
   - 新增 `@app.route("/node-image/<node_id>")` 路由
   - 从 002 项目资源 中按节点编号匹配图片
   - 无图片时返回军旗占位 SVG
   - 图片放在 `panel-image` 区域（固定在面板顶部，不随内容滚动）
   - 占位 SVG 在 001 项目源码/static/images/flag-placeholder.svg

2. **数据清洗：active_sections 标志系统**（V003#3.1+3.3#步骤3）
   - 每个节点新增 `active_sections` JSON 字段，记录哪些内容段有实际内容
   - 清洗 node_data.json：去掉"字段名：无"前缀，空字段设为 null
   - 数据库 node 表新增 `active_sections` TEXT 列
   - 重新导入 29 个节点数据

3. **前端按 active_sections 过滤显示**
   - 5 步主流程（time→battle→meeting→event→significance）只显示 active 的步骤
   - 扩展信息（site→poem→story→people→spark）只显示 active 的字段
   - 步骤指示器只计数 active 步骤
   - 翻页在 active 步骤之间切换

4. **项目版本管理初始化**
   - VERSION: 1.0.0
   - CHANGELOG.md
   - ROADMAP.md（V1.1→V3.0 规划）
   - .gitignore
   - Git 仓库 init + first commit

### 修改的文件

- 001 项目源码/app.py — 新增 CREATE TABLE active_sections 列 + node-image 路由
- 001 项目源码/static/js/nodes.js — 重写至 380 行：active_sections 过滤 + 图片分离 + 步骤跳转
- 001 项目源码/static/css/style.css — 替换节点图片区域样式为 panel-image 固定布局
- 001 项目源码/templates/index.html — 新增 panel-image div
- 001 项目源码/data/node_data.json — 清洗数据 + active_sections
- 001 项目源码/static/images/flag-placeholder.svg — 占位军旗

### 已测试

- [x] Python 语法验证
- [x] API 返回 active_sections 字段
- [x] 有图片节点正确显示
- [x] 无图片节点显示军旗占位
- [x] 无内容字段不显示（如黎平无 poem_article）

---

## 照片填充（V1.1.0 第二阶段）

### 完成事项

1. **29 个节点全部填充历史照片，共 97 张**
   - 来源：百度百科 CDN (`bkimg.cdn.bcebos.com/pic/`) + 百度图片搜索 API (`image.baidu.com/search/acjson`)
   - 文件系统：`002 项目资源/{节点名称}/` 目录
   - 数据库：`media` 表 97 条记录，`type=image, status=approved`
   - 前端 `node-image/<node_id>` 路由直接从 002 目录读取

### 每节点照片数

| 数量 | 节点 |
|------|------|
| 6张 | 通道会议 |
| 5张 | 长汀、黎平、懋功会师 |
| 4张 | 于都、瓮安、遵义 |
| 3张 | 瑞金、湘江、扎西、苟坝、四渡赤水、金沙江、大渡河、夹金山、两河口、芦花、松潘草地、过草地、巴西、俄界、腊子口、哈达铺、榜罗镇、六盘山、吴起镇、会宁、将台堡 |
| 1张 | 泸定桥 |

### 总览

- **002 项目资源** 中相邻文件夹名括号一致，显示为统一格式
- 之前只 4 个节点有照片，现在全部填满
- 部分节点的文件名后缀差异（如 `changting_1.jpg` vs `福建长汀_1.jpg`），不影响功能

### Git

- `c3a98ca` — feat(media): fill all 29 nodes with photos + insert into database

### 清理

- 临时 Python 脚本已删除（test_baike.py, dl_changting.py, dl_baike.py, dl_batch.py, dl_batch2.py, dl_batch3.py, dl_missing.py, dl_tongdao.py, test_dl.py, test_dl2.py, test_api.py, test_app.py, verify_dl.py, check_links.py, node_image_inventory.txt, blanks_report.txt, doc_design_extract.txt）
- `Doxyfile` 中已添加 EXCLUDE 排除 Agent 工作区文件（AGENTS.md, SOUL.md 等）

### 未完成

- 节点名称文件夹内文件名尚未统一标准化
- 尚未将 Doxygen 中的 Agent 文件排除结果截图给用户确认

---

## 文件名清理 + 数据库同步（V1.1.0 第三阶段）

### 完成事项

1. **用户手动替换照片后处理**
   - 用户将照片放入 `002 项目资源/` 各文件夹，共 78 张
   - 清理乱码文件名：`u=xxxx&fm=xxxx.webp` → `img_1.jpg`、`img_2.webp` 等干净格式
   - 保留中文命名（如 `rj_1.jpg`）和已规范化的文件名不变
   - 数据库 `media` 表清空后重新插入 78 条记录，匹配实际文件
   - `node-image/<node_id>` 测试通过全部 29 节点

2. **应用标题修正（V003 §4.1）**
   - 原设计文档要求：**左上角** 显示 "我走长征路 — 交互式长征文化数字平台"
   - 此前误写为 "重走长征路 — 长征文化数字地图"
   - 修改内容：
     - `<title>`: 重走长征路 → **我走长征路 — 交互式长征文化数字平台**
     - 加载封面: 重 走 长 征 路 → **我 走 长 征 路**
     - 标题栏 h1: 重走长征路 → **我走长征路**
     - 标题栏 subtitle: 长征文化数字地图 → **交互式长征文化数字平台**
     - 标题栏位置: 居中 → **左上角** (`left: 16px; transform: none`)
   - 涉及文件: `index.html`、`style.css`
   - 已重启 Flask 应用

### 设计规范（不可违背）

- 设计文档要求必须遵循，不能自行发挥
- 标题来自 V003 §4.1 左上角标识规范

### 临时脚本

- 保留供参考：`rename_clean.py`、`rename_clean2.py`、`sync_media.py`、`test_img.py`
- 图片相关脚本可随时删除


---

## V1.0.1 版本发布

### 变更清单
1. **设计文档修正**（3处）：内同→内容、但前→当前、删除孤立段落
2. **§3.3 步骤2**：节点名字/时间段加入固定区，显示位置=激活点左侧
3. **§4.1**：标题艺术字、地图自动缩放至路线
4. **§4.2**：5按钮→7按钮（新增星火拾遗）、详细行为规范
5. **§4.4**：漫游机制完全重写（3s/2s/1s、8s、语音完整朗读、暂停/跳过/重头/退出）
6. **面板动画**：slide-in 转场
7. **TTS高亮**：朗读时金色边框+背景高亮
8. **标题字体**：楷体艺术字
9. **星火拾遗按钮**：右侧控制栏第7个
10. **退出漫游按钮**：漫游子菜单
11. **军队选择器**：点击外部关闭
12. **VERSION**：1.0.0→1.0.1

### Git
- 9b6dbc — feat: V1.0.1 design update + UI improvements


---

## V1.0.1 第二波改动

### Git commit: d923dcb
### 修复
1. **标题间距**：我走长征路 改为 我 走 长 征 路
2. **地图自动缩放**：Promise.all等待节点+路线加载完后fitBounds
3. **弹出面板不显示bug**：HTML中style=display:none移除，改用CSS类
4. **设计文档错别字**：内同->内容、但前->当前、删除孤立段落

### 新增
5. **星火拾遗完整对话框**：新建节点/补充既有节点两种模式
6. **全字段表单**：标题、地点、经纬度、时间、战役、会议等所有节点域
7. **文件上传**：图片(多选)、视频、音频、文档
8. **上传者信息**：姓名、电话（选填）
9. **后端API**：POST /api/v1/sparks/full，存入待审核
10. **经纬度地图拾取**：点击输入框后在地图选点
