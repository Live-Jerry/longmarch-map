
## 2026-08-15 生产资源无法预览修复

## 问题（8:32 用户报告：所有的资源都无法预览）

生产缺三样东西 + 数据未同步：
1. **browse 路由**：生产 /opt/zhichangxuan/ app.py 只有 136 行（无 /resources/browse/<path> 路由）——08-13 加的 browse 路由代码只在 dev/本地，生产 Python 代码一直没部署（昨天升级只部署了 base.html/style.css/main.js 前端三文件，没部署 .py）
2. **resource_files.html 模板**：生产 templates 缺（dev 有）
3. **static/res/ 目录（3.1G）**：生产缺（dev 有，6 子目录 lunyu/xunzi/zengguang/shige/ershisi/chuxue）
4. **生产数据库 resource_link 表 id 2-7 的 share_url 为空**（dev 已填 /resources/browse/xxx）→ 卡片渲染不出「前往资源」链接；column_name 生产是旧值「必读经典三级导读」

## 修复（全服务器端，本地代码零改动）

- app.py 打补丁：sed 从 dev 提取 97-117 行 browse 路由块插入生产 96 行后（heredoc python 中文路径经管道损坏，改 sed 按行号字节操作）；第 21 行栏目名同步 dev（必读经典三级导读→必读经典，与数据库匹配）
- cp dev 的 resource_files.html → 生产
- cp -r dev 的 static/res（3.1G）→ 生产（rsync --delete 保证一致）
- 生产数据库 resource_link id 1-7 全字段同步 dev（备份 zhichangxuan.db.bak_TS）
- 重启 zhichangxuan，全部验证通过：browse/lunyu 200 列 20 mp3、mp3 200 audio/mpeg、路径穿越 404、PDF 200 application/pdf、资源中心 5 browse 链接 + 24 PDF 链接
- 教训：**服务器 Python 代码落后本地 git 是一个隐患**——dev 一直在部署 .py（18:08 重启前 dev app.py 就是新版），生产只在部署前端文件。以后升级生产必须检查 .py/.html 模板是否与 dev 一致（diff 或哈希比对）
- 生产 app.py 现与 git HEAD 完全一致（browse 块 + 必读经典栏目名）


### 抽屉最终版（v=246，已上生产）
用户反复迭代后的定稿规格（每一轮教训，务必记住）：
- 面板：**无底（透明）**，只剩 1px 金边框 + 顶部 2px 缃黄金线 + 圆角 14px + 阴影；悬浮卡片式：top:71px（= header 高，正好从金线下缘开始）、right:10px、width:min(150px,41vw)（300px 减半！）、max-height:calc(100vh-81px)、内部滚动、金色滚动条（::-webkit-scrollbar-thumb rgba(232,197,71,.55)）
- 按钮格（15 项全部）：**不透明实底** var(--xuan-qing) rgb(28,28,46) + 1px 金边 rgba(232,197,71,.35) + 圆角 8px + 文字居中 + 16px 月白字
- hover：**背景一律不变，只文字变缃黄**（全局规则去掉 background: rgba(232,197,71,.18)——用户明确批评"两个一变彻底看不清"，PC 悬浮和移动点击残留都适用）
- 遮罩 rgba(0,0,0,.45)，点遮罩/Esc/菜单项收起，body.nav-open 锁滚动

### 理解偏差教训（用户发火 4 次，根因）
1. "透明度太高减半"= 按钮格填充透明度 15%（淡填充）→ 用户又改"不透明，透明影响对比度"（最终：按钮实底）
2. "不要整块渐变底"= 面板**不要底**（透明），不是"底不渐变"——语义反复横跳，最终用户明说"金色边框之外的底去掉"
3. **改 CSS 内容必须 bump 版本参数**！v240→v241 时我没 bump，用户看到旧缓存怒斥"你说了也没改啊！！"——每次改 style.css 必 bump base.html 里的 ?v=
4. 用户截图看的是旧缓存版本时，先怀疑版本参数没变，别急着改代码

### 生产升级（按版本管理规范 V4.1 第 6 节执行）
- 通读《100通用管理/项目版本通用管理规范.docx》：三个位置（本地/远程仓库/服器Dev+服务器prd）、tag 规范 <项目代码>prd+版本号+last-good、<项目代码>dev+版本号+rc、回滚流程 7.1-7.3
- 哈希比对确定生产当前 = 067aa0d（v238：页脚去重已上，抽屉未上）；注意生产 css 与 git 曾有偏差（5b36048 不在任何提交）——回滚基准以 git 067aa0d 为准
- Tags：ZCXprd+2.3.7+last-good @ 067aa0d、ZCXdev+2.3.8+rc @ f05349f，均已 push
- 部署 /opt/zhichangxuan/：base.html(v246)+style.css(v246)+main.js(v236)，先 .bak_TS 备份再覆盖，restart zhichangxuan（active），哈希三文件全一致
- 实测 zhichangxuan.com：桌面 1000px 两行 8+7 单行、hover 只变字色；375px 抽屉 150px 宽/71px 顶对齐/无底/按钮实底居中/906>583 可滚动/遮罩正常
- 知常轩生产与测试现在都是完整 V2.3.8；生产 main.js 从 v235 升到 v236（抽屉 JS）

### 提交链（8-14 晚，均 push develop）
9df7af7(v240 悬浮卡片) → 493ff3b(顶对齐 71px+去渐变底) → 5335fad(v241) → 267bce6(v242 面板无底) → 6e271fc(v244 按钮填充 15%) → 14a5bdc(v245 按钮不透明实底) → f05349f(v246 hover 只变字色) → 生产部署完成

### 关键实测值（以后直接复用）
- header 高 71px（含 3px 金线）= 抽屉 top 对齐值
- 375px 下抽屉: rect 215-365 x 71-657；scrollH 906 > clientH 583
- 抽屉宽度: min(150px, 41vw)；间距: 距边 10px
- PowerShell 双引号内 $_ 会被吞（foreach 炸），用单引号或 python
