# -*- coding: utf-8 -*-
"""
@file    interfaces/spark_interface.py
@brief   星火拾遗接口定义
@details 定义星火内容提交、查询、审核的 API 规范。
@author  长征文化数字地图项目组
@date    2026-07-15

基础 URL: /api/v1/sparks
"""

# =============================================================================
# GET /sparks — 星火列表（公众）
# =============================================================================
"""
GET /sparks

描述: 获取已审核通过的星火内容列表（游客可见）。

Query: page, per_page, node_id
响应: { code, data: paginated_sparks }
"""


# =============================================================================
# GET /sparks/<id> — 星火详情
# =============================================================================
"""
GET /sparks/<id>

描述: 获取单个星火内容详情。

说明: 非管理员只能查看 status=approved 的内容。
"""


# =============================================================================
# POST /sparks — 提交星火内容
# =============================================================================
"""
POST /sparks

描述: 提交新的星火拾遗内容（需登录）。

权限: 需要登录用户

请求体:
{
    "title":      "老红军的回忆",      // 必填
    "content":    "我是xxx，1934年...", // 可选
    "node_id":    "001",               // 关联节点（可选）
    "media_type": "text",              // media 类型（可选）
    "file_path":  "uploads/...",       // 已上传文件的路径（可选）
    "source":     "口述历史"           // 资料来源（可选）
}

响应 (201):
{
    "code": 0,
    "data": { ...spark_object }
}
"""


# =============================================================================
# GET /sparks/my — 我的投稿
# =============================================================================
"""
GET /sparks/my

描述: 获取当前用户提交的所有星火（包含待审核）。

权限: 需要登录
"""


# =============================================================================
# GET /sparks/pending — 待审核列表（管理员）
# =============================================================================
"""
GET /sparks/pending

描述: 获取所有待审核的星火内容。

权限: admin/super

响应: { code, data: [spark_items] }
"""


# =============================================================================
# PUT /sparks/<id>/review — 审核星火（管理员）
# =============================================================================
"""
PUT /sparks/<id>/review

描述: 审核星火内容（通过或拒绝）。

权限: admin/super

请求体:
{
    "status":        "approved" | "rejected",
    "admin_comment": "审核意见（可选）"
}
"""


# =============================================================================
# DELETE /sparks/<id> — 删除星火（管理员）
# =============================================================================
"""
DELETE /sparks/<id>

描述: 删除星火内容（管理员）。

权限: admin/super
"""
